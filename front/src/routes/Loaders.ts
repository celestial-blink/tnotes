import { defer, redirect } from "react-router-dom";
import type { LoaderFunctionArgs } from "react-router-dom";

export const LoaderSession = ({ request, params }:LoaderFunctionArgs) => {
    return {}
}
