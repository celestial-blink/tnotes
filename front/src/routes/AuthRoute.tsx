import type { ReactNode } from "react";
import type { NavigateProps } from "react-router-dom";

const AuthRoute = ({children,  ...rest} : { children:ReactNode  } & NavigateProps ) => {
    return (
        null
    );
}

export default AuthRoute;
